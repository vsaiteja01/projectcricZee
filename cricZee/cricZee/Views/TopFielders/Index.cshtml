@model IEnumerable<cricZee.Models.TopFielder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Player)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Matches)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Catches)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Player)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Matches)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Catches)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Player">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Player">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Player">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
